<div class="container my-3 mx-auto">
  <div class="col-12 col-md-10 mx-auto">
    <div class="filter">
      <form action="/">
        <div class="input-group mb-3">
          <select class="custom-select" name="viewBy" onchange="this.form.submit()">
            <option selected disabled hidden>View by...</option>
            <option value="">全部類別</option>
            {{#each category_info}}
            <option value="{{this.name}}" {{#isEqual this.name ../viewBy}} selected {{/isEqual}}>{{this.label}}</option>
            {{/each}}
          </select>
        </div>
      </form>
    </div>

    <div class="total-expense my-5">
      <div class="row">
        <div class="col-4 total-expense-title">
          <h5>總金額</h5>
        </div>
        <div class="col-8 total-expense-num text-left">
          <h1> {{total_expense}}</h1>
        </div>
        <div id="myDiv"></div>
      </div>
    </div>

    <div class="expense-table">
      <table class="table table-striped">
        <colgroup>
          <col class="col-md-1">
          <col class="col-md-5">
          <col class="col-md-2">
          <col class="col-md-4">

        </colgroup>
        <tbody>
          {{#each records}}
          <tr>
            <td class="text-center align-middle">
              {{{this.category_icon}}}</td>
            <td>
              <div class="category-name d-flex flex-column">
                <div class="category m-b">
                  {{this.name}}
                </div>
                <div class="date mt-0">
                  {{this.date}}
                </div>
              </div>
            </td>
            <td class="text-center align-middle">{{this.cost}}</td>
            <td class="text-center align-middle">
              <a class="btn btn-primary btn-sm" href="/records/{{this._id}}/edit">修改</a>
              <a href="#myModal-{{this._id}}" class="trigger-btn btn btn-primary btn-sm" data-toggle="modal">刪除</a>


    </div>
    </td>
    </tr>
    <div id="myModal-{{this._id}}" class="modal fade">
      <div class="modal-dialog modal-confirm">
        <div class="modal-content">
          <div class="modal-header flex-column">
            <div class="icon-box">
              <i class="fas fa-exclamation-triangle fa-lg "></i>
            </div>
            <h4 class="modal-title w-100">警告</h4>
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          </div>
          <div class="modal-body">
            <p>確定要刪除這筆資料嗎?<br>刪除的資料無法復原</p>
          </div>
          <div class="modal-footer justify-content-center">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">取消</button>
            <form action="/records/{{this._id}}?_method=DELETE" method="POST">
              <button class="btn btn-danger" type="submit">確認刪除</button>
            </form>
          </div>
        </div>
      </div>
    </div>
    {{/each}}
    </tbody>
    </table>
  </div>
  <div class="new-expense d-flex justify-content-center mt-5">
    <a class="btn btn-primary btn-lg" href="/records/new">新增支出</a>
  </div>
</div>
</div>